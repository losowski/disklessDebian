TODO:
-) Build script split to run in chroot
-) Setup DNS service
-) Change all services to use DNS names
-) Setup UnionFS for /tmp drive
-) Harden the OS to be read only (reliant on unionfs)
-) Configure default to run a standard image via netboot
-) unionfs for hostname specific NFS drive access

PENDING:


DONE:
-) Boot with correct init script selected
-) Change /etc/fstab for home NFS drive